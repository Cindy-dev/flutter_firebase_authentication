//this class will serve as a local ram to hold data through out the app lifecycle

import 'package:flutter_firebase_authentication/data/model/user.dart';

class DummyData{
static User userDummyData = User();
}